package model.persistence.storage;

import model.domain.level.Level;

public class LevelRestarter {
    public void restart(Level level) {
        // Logic to restart the level
        // Resets the save state to the initial state
    }
}
